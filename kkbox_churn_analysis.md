# KKBOX 音乐订阅用户流失预测

**数据来源**：Kaggle KKBOX Music Streaming Churn Prediction  
**分析工具**：Python（pandas、scikit-learn、statsmodels、matplotlib）  
**分析时间**：2017年3月流失预测窗口

---

## 一、项目背景

KKBOX 是台湾最大的音乐流媒体平台，采用月付/季付/年付订阅制。本项目基于用户的历史订阅行为，构建流失预测模型，并通过三轮特征迭代量化不同维度特征对流失的预测贡献。

**流失定义**（官方）：用户订阅到期后 30 天内无新的有效订阅，判定为流失。  
**关键注意**：`is_cancel=1`（主动取消）≠ 流失。用户可能取消后重新订阅，取消字段不能作为流失标签。

---

## 二、数据准备

### 2.1 数据来源

| 文件 | 用途 | 规模 |
|------|------|------|
| transactions.csv + transactions_v2.csv | 用户付费记录（核心） | 合并后 22,975,416 条 |
| train_v2.csv | 流失标签（2017年3月窗口） | 970,960 用户 |
| members_v3.csv | 用户基础信息 | 6,769,473 用户 |

transactions v1 覆盖 2015-01-01 至 2017-02-28，v2 延伸至 2017-03-31，两者为时间上的互补关系，合并后去重使用。

### 2.2 数据清洗

**membership_expire_date 异常值处理**

通过年度分布发现两类异常：
- `< 2015-01-01`：9,717 条，transaction_date 从 2015 年才开始，此前的 expire_date 为历史遗留脏数据
- `> 2018-12-31`：9,718 条，transaction_date 最晚为 2017-03-31，不可能产生 2019 年后的到期日，为系统占位符

基于 transaction_date 实际范围推断合理边界，丢弃 19,435 条（0.085%），对分析无实质影响。

**train_v1 vs train_v2**

train.csv 是 2017 年 2 月流失标签，train_v2.csv 是 2017 年 3 月流失标签，两份标签对应不同预测窗口，88% 用户重叠但流失状态可能不同，不能合并，本项目使用 train_v2。

### 2.3 快照日期选取

```
特征计算期：2015-01-01 ～ 2017-02-28（快照日）
标签观察期：2017年3月到期用户 + 30天续费窗口
```

快照日定为 2017-02-28，确保特征在前、标签在后，无数据泄漏。

### 2.4 最终数据集

- 丢弃快照日前无任何 transaction 记录的用户（2,524 条，0.26%）
- 最终数据集：**968,436 用户，流失率 8.9%**
- 按 8:2 划分训练集和测试集，stratify 保持流失比例一致

---

## 三、RF 特征工程

### 3.1 订阅场景下的 RFM 改造

标准电商 RFM 无法直接用于订阅产品，需要重新定义：

| 维度 | 电商定义 | 订阅改造 | 原因 |
|------|----------|----------|------|
| R | 最近购买距今天数 | 最后一次 membership_expire_date 距快照日天数 | 付款日和服务有效期之间有 gap，到期日更能反映当前订阅状态 |
| F | 购买次数 | transaction 条数 | 每条 transaction 代表一次付费行为 |
| M | 消费金额 | 丢弃 | 固定定价，M 没有区分度 |

**Recency 的特殊处理**：约 48.2% 的用户 recency 为负数（快照日时订阅尚未到期），这是合法的，负数越大表示离到期越远，是最健康的状态，保留不截断。

### 3.2 RF 分层流失率

| R_score | F_score | 流失率 | 用户数 | 含义 |
|---------|---------|--------|--------|------|
| 3 | 1 | 14.9% | 92,780 | 高危：快照日还在订阅但续费次数少 |
| 1 | 1 | 13.7% | 111,421 | 次高危：到期较久且续费次数少 |
| 1 | 3 | 4.2% | 89,241 | 健康：老用户续费次数多 |
| 2 | 3 | 4.9% | 103,456 | 健康：中等 recency + 高频续费 |

**F 的规律非常清晰**：每个 R 层内，F 越高流失率越低，单调递减。

**R 的反直觉表现**：R_score=3（快照日还在订阅中）的用户流失率反而最高。原因是 train_v2 是特殊的同质群体（所有人都是 3 月到期），R 在这个群体里的区分度被压缩，不能简单套用电商 RFM 的直觉。

---

## 四、特征诊断

在建模之前，对全部 14 个特征进行三项诊断：相关性分析、显著性检验、多重共线性检验。

### 4.1 相关性热力图

与 is_churn 相关性最强的特征：

```
last_auto_renew：-0.42  ← 最强保护因子
avg_plan_days：  +0.35  ← 最强风险因子
frequency：      -0.15
recency：        -0.12
```

特征间值得注意的相关性：

```
tenure_days vs gender_known：  0.46
discount_rate vs is_free_user：0.42  （合理，免费用户折扣率高）
frequency vs tenure_days：     0.45  （注册越久续费次数越多）
last_auto_renew vs avg_plan_days：-0.34  （开自动续费的人倾向月付）
```

所有特征间相关系数均未超过 0.5，无严重多重共线性迹象。

### 4.2 显著性检验

连续特征使用 Mann-Whitney U 检验，类别/二值特征使用卡方检验：

| 特征 | 检验方法 | p 值 | 显著性 |
|------|----------|------|--------|
| recency | Mann-Whitney | 5.43e-112 | *** |
| frequency | Mann-Whitney | 0.000 | *** |
| avg_plan_days | Mann-Whitney | 0.000 | *** |
| cancel_rate | Mann-Whitney | 3.18e-145 | *** |
| tenure_days | Mann-Whitney | 2.69e-31 | *** |
| **discount_rate** | Mann-Whitney | **0.865** | **ns** |
| last_auto_renew | 卡方 | 0.000 | *** |
| is_free_user | 卡方 | 0.000 | *** |
| gender_known | 卡方 | 0.000 | *** |
| via_3.0 | 卡方 | 0.000 | *** |
| via_4.0 | 卡方 | 0.000 | *** |
| via_7.0 | 卡方 | 0.000 | *** |
| via_9.0 | 卡方 | 0.000 | *** |
| **via_0.0** | 卡方 | **0.835** | **ns** |

**discount_rate** 和 **via_0.0** 两个特征不显著，从最终模型中移除。

### 4.3 VIF 多重共线性检验

| 特征 | VIF | 判断 |
|------|-----|------|
| last_auto_renew | 10.41 | ⚠️ 略高，但作为最强预测因子予以保留 |
| via_7.0 | 9.86 | one-hot 结构性共线性，可接受 |
| frequency | 6.87 | 中度，与 tenure_days 的自然业务相关 |
| via_9.0 | 6.28 | one-hot 结构性共线性，可接受 |
| tenure_days | 4.99 | 正常 |
| gender_known | 3.52 | 正常 |
| via_3.0 | 2.72 | 正常 |
| avg_plan_days | 2.26 | 正常 |
| via_4.0 | 1.47 | 正常 |
| recency | 1.42 | 正常 |
| cancel_rate | 1.19 | 正常 |
| is_free_user | 1.00 | 正常 |

last_auto_renew VIF=10.41 略超阈值，但为全模型最强预测因子，移除会显著损失模型性能，予以保留并在此注明。

**最终入模特征：12 个**（移除 discount_rate 和 via_0.0）

---

## 五、模型迭代

三轮递进，每轮回答一个不同的问题：

| 轮次 | 特征 | 训练集 AUC |
|------|------|-----------|
| 第一轮：RF 基础模型 | recency, frequency | 0.675 |
| 第二轮：+ 行为特征 | + auto_renew, cancel_rate, avg_plan_days, discount_rate, is_free_user | 0.812 |
| 第三轮：+ 人口特征 | + tenure_days, gender_known, 注册渠道 | 0.825 |

行为特征带来的增量（+0.137）远大于人口特征（+0.013），说明**用户怎么续费比用户是谁更能预测流失**。

> 注：三轮迭代 AUC 均在全量数据上计算，用于横向比较特征增量；最终模型性能以测试集（20% holdout）为准，见第六节。

---

## 六、模型评估

### 6.1 三模型对比（测试集）

在 12 个特征上，对比逻辑回归和随机森林在不同策略下的表现：

| 模型 | 策略 | AUC | Precision | Recall | F1 |
|------|------|-----|-----------|--------|----|
| 逻辑回归 | 默认阈值 0.5 | 0.827 | 0.77 | 0.22 | 0.34 |
| 随机森林 | class_weight='balanced' | 0.885 | 0.40 | 0.73 | 0.52 |
| 随机森林 | 阈值调整（0.26） | 0.884 | 0.58 | 0.56 | 0.57 |

**逻辑回归（默认阈值）**：precision 高但 recall 极低，漏掉了 78% 的流失用户。根本原因是流失率仅 9%，默认阈值 0.5 在类别不平衡数据集上天然偏向多数类。

**随机森林（balanced）**：class_weight='balanced' 对少数类加权，recall 大幅提升至 0.73，但 precision 跌至 0.40，60% 的预测流失用户实际未流失。

**随机森林（阈值调整）**：不依赖 class_weight，改用 precision-recall curve 找 F1 最大的阈值（0.26），precision 和 recall 相对平衡，F1 达到 0.57，是三者中最均衡的选择，作为最终模型。

### 6.2 阈值选择的业务含义

阈值不是技术参数，是业务决策：

```
误报（预测流失但实际未流失）→ 对这个人发了不必要的优惠券，损失一点成本
漏报（预测未流失但实际流失）→ 没有挽留，直接失去这个用户
```

挽留成本低（如只发邮件）→ 选 balanced，接受更多误报，尽量不漏；  
挽留成本高（如一对一电话）→ 调高阈值，减少误报，只干预高确信用户。

---

## 七、核心发现

### 7.1 特征系数与 Odds Ratio

标准化系数用于比较特征之间的相对重要性；Odds Ratio 用于解释单个特征的业务含义，两者回答不同问题，均有必要。

**保护因子（OR < 1，降低流失风险）**

| 特征 | 标准化系数 | OR | 业务解读 |
|------|-----------|-----|---------|
| last_auto_renew | -0.771 | 0.463 | 开启自动续费，流失几率降低 54% |
| recency | -0.261 | 0.771 | 到期日每近一天，流失几率降低 23% |
| frequency | -0.120 | 0.889 | 每多续费一次，流失几率降低 11% |
| via_7.0 | -0.038 | 0.961 | 渠道 7 用户留存略好 |

**风险因子（OR > 1，提高流失风险）**

| 特征 | 标准化系数 | OR | 业务解读 |
|------|-----------|-----|---------|
| avg_plan_days | +0.384 | 1.462 | 套餐时长每增加一单位，流失几率上升 46% |
| cancel_rate | +0.255 | 1.289 | 历史取消率越高，流失几率上升 29% |
| is_free_user | +0.098 | 1.103 | 免费用户面临首次付费决策，流失风险更高 |
| tenure_days | +0.085 | 1.086 | 注册越久反而略微更容易流失 |
| via_4.0 | +0.057 | 1.058 | 渠道 4 用户流失风险最高 |

### 7.2 发现一：自动续费是最强单一信号

`last_auto_renew` 标准化系数 -0.771，是第二名的 3 倍，OR=0.463，与 is_churn 的相关系数 -0.42 也是所有特征中最高的。

开启自动续费的用户把续费决策"外包"给了系统，流失摩擦力最小；关闭自动续费的用户只要有一次忘了或懒了就会流失。这是产品设计层面最直接的留存杠杆。

### 7.3 发现二：套餐越长流失率越高

| 套餐时长 | 流失率 | 用户数 |
|----------|--------|--------|
| ≤30 天（月付） | 6.6% | 890,665 |
| 31-90 天 | 21.1% | 56,891 |
| 91-180 天 | 59.7% | 9,881 |
| >180 天（年付+） | 83.5% | 10,997 |

流失率单调递增，OR=1.462，Mann-Whitney p=0.000。与 frequency 的相关系数仅 -0.25，排除多重共线性为主因。

真实机制：月付用户每个月都在主动"投票续费"，能坚持下来的都是真正活跃的用户；年付用户一次性购买后，中间一年几乎没有与平台重新建立连接的机会，到期时已经习惯没有这个服务。

### 7.4 发现三：注册渠道影响用户留存质量

| 渠道 | OR | 显著性 | 留存表现 |
|------|----|--------|----------|
| 渠道 7 | 0.961 | *** | 最好 |
| 渠道 9 | 0.992 | *** | 次好 |
| 其他渠道（via_0） | — | ns | 无区分度，已移除 |
| 渠道 3 | 1.026 | *** | 次差 |
| 渠道 4 | 1.058 | *** | 最差 |

产品决策价值：加大渠道 7 的获客投入；审查渠道 4 的用户来源是否以促销驱动为主，并对该渠道新用户做针对性的早期留存干预。

局限性：渠道编号的具体含义未公开，无法直接对应业务渠道名称，需结合平台内部数据才能落地执行。

### 7.5 发现四：行为特征远比人口特征重要

```
第一轮（RF）：用户的历史订阅轨迹          AUC 0.675
第二轮（+行为特征）：用户怎么续费          AUC 0.812  ← 大跳跃 +0.137
第三轮（+人口特征）：用户是什么样的人      AUC 0.825  ← 小增量 +0.013
```

在订阅产品里，用户的行为意图（是否开启自动续费、是否主动取消、套餐时长选择）对流失的预测贡献远超人口属性（注册时长、性别、城市）。

---

## 八、方法论反思

**is_cancel ≠ 流失**  
主动取消不等于流失。KKBOX 的流失定义是到期后 30 天内无新订阅，这个业务逻辑在建模前必须理解清楚，否则标签定义从根上就错。

**RFM 定义需要领域适配**  
电商的 R/F/M 定义不能直接套用订阅产品：M 因固定定价被丢弃；R 改用 membership_expire_date 而非 transaction_date；F 的含义从"购买次数"变为"续费次数"。标准框架是起点，不是终点。

**快照日的时间隔离**  
快照日的选取直接决定是否存在数据泄漏。用 transaction_date 筛选特征计算期（行为发生时间），用 membership_expire_date 计算 recency（行为结果），两者职责不同，不能混用。

**显著性检验驱动特征选择**  
discount_rate（p=0.865）和 via_0.0（p=0.835）不显著，移除后模型 AUC 从 0.8829 微升至 0.8843，验证了"噪声特征不会提升模型，去掉更干净"的原则。这与 Rees46 项目中 10 特征逻辑回归优于 18 特征随机森林的发现一致。

**Recency 的反直觉现象**  
R_score=3 的用户流失率反而最高，目前没有完整的解释。这是订阅场景中 RFM 与电商场景最显著的差异之一，留作后续分析方向：可尝试改用 last_transaction_date 重新定义 R，或引入续费间隔特征替代静态 recency。

**多变量系数的正确解读**  
frequency 在 RF 模型里是最重要的特征（系数 -0.591），但在全模型里系数缩水至 -0.120。这不是矛盾，而是 avg_plan_days 分担了 frequency 的解释力。单变量重要性和多变量系数不能直接比较。

**标准化系数 vs Odds Ratio**  
两者回答不同问题：标准化系数比较特征间的相对重要性；OR 解释单个特征的业务含义。portfolio 中两者都需要，前者面向技术审查，后者面向业务解读。

**阈值是业务决策，不是技术参数**  
默认阈值 0.5 在类别不平衡数据集上天然偏向多数类，导致 recall 极低。阈值的选取应由业务场景决定：挽留成本低时降低阈值提高 recall，挽留成本高时提高阈值保证 precision。

---

## 下一步方向

- 引入 user_logs 听歌行为特征，验证流失用户是否在到期前存在可观测的活跃度下降信号
- 用 last_transaction_date 重新定义 Recency，验证能否解决反直觉现象
- 对 transaction 行为序列做更细粒度的时序特征提取，包括续费间隔变化、套餐时长趋势等动态特征
- 对注册时间做细粒度拆解（year/month/weekday），给树模型提供更多切分维度
