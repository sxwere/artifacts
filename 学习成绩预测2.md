# 葡萄牙学生成绩预测分析

**Author:** sxwere  
**Dataset:** Student Performance Dataset (UCI Machine Learning Repository)  
**Goal:** 预测学生期末成绩 G3，并找出影响成绩的关键因素

---

## 目录

1. [项目背景](#1-项目背景)
2. [数据概览](#2-数据概览)
3. [特征工程](#3-特征工程)
4. [建模过程](#4-建模过程)
5. [SHAP 可解释性分析](#5-shap-可解释性分析)
6. [结论与建议](#6-结论与建议)

---

## 1. 项目背景

本项目使用葡萄牙两所高中的真实学生数据，涵盖家庭背景、学习习惯、社交行为等 30+ 个变量，目标是预测学生的期末数学成绩（G3，满分 20 分）。

与合成数据集不同，真实数据具有以下挑战：
- 特征之间存在潜在共线性
- 部分变量为类别型，需要编码处理
- 目标变量分布不均匀，存在大量 0 分（放弃考试的学生）
- R² 天花板远低于合成数据，0.25 左右即属正常

---

## 2. 数据概览

- **样本量：** 634 条
- **特征数：** 25 个（编码及特征工程后）
- **目标变量：** G3（0–20 分）

### 目标变量分布

![G3分布](<images/G3 分布直方图.png>)

G3 分布呈现双峰特征：一个峰值在 0 分附近（放弃考试的学生），另一个峰值在 10–12 分区间。这种分布意味着模型需要同时处理两类完全不同的学生群体，是 R² 偏低的核心原因之一。

---

## 3. 特征工程

### 3.1 类别变量编码

- **Label Encoding：** 二元类别变量（school、sex、address、higher 等）
- **One-Hot Encoding：** 多类别变量（Mjob、Fjob、reason、guardian）

### 3.2 聚合特征构造

基于领域知识手动构造 3 个聚合特征：

| 特征名 | 构造方式 | 含义 |
|--------|----------|------|
| `parent_edu` | Medu + Fedu | 父母教育水平综合 |
| `alcohol` | Dalc + Walc | 周内 + 周末饮酒量总和 |
| `social_risk` | goout + Dalc + Walc | 社交风险综合评分 |

> 聚合特征的意义在于将领域知识编码进模型——XGBoost 不会自动理解"总饮酒量"这个概念，但人类知道这比单独看工作日或周末更能反映一个学生的生活习惯。

### 3.3 共线性检验（VIF）

对所有特征进行方差膨胀因子检验，最高 VIF < 10，共线性在可接受范围内，无需剔除特征。

---

## 4. 建模过程

### 4.1 线性回归基准

首先建立线性回归作为基准模型，验证数据质量和特征有效性。

**模型诊断：**

![残差图](<images/残差图.png>)

![Actual vs Predicted](<images/Actual vs Predicted 图.png>)

残差图显示在低分段存在系统性偏差，说明成绩和特征之间存在非线性关系，线性回归已到瓶颈。

### 4.2 XGBoost 调参

使用 GridSearchCV 进行 5 折交叉验证调参，搜索空间：

```python
param_grid = {
    "n_estimators":     [50, 100, 200],
    "max_depth":        [2, 3, 4],
    "learning_rate":    [0.05, 0.1, 0.2],
    "subsample":        [0.7, 0.9],
    "colsample_bytree": [0.7, 0.9],
}
```

**最优参数：**

```
colsample_bytree: 0.9
learning_rate:    0.05
max_depth:        4
n_estimators:     100
subsample:        0.7
```

`max_depth=4` 和 `subsample=0.7` 说明调参在压制过拟合——对于只有 634 条样本的数据集，树模型天然容易过拟合，保守的参数配置更有利于泛化。

### 4.3 三模型对比

| 模型 | R² | MAE | RMSE |
|------|----|-----|------|
| 线性回归 | 0.2255 | 2.050 | 2.520 |
| XGBoost 默认参数 | 0.1839 | 2.055 | 2.586 |
| **XGBoost 调参 + 聚合特征** | **0.2848** | **1.917** | **2.421** |

**关键发现：**
- XGBoost 默认参数比线性回归还差，说明在小样本数据上调参是必须的，不能直接用默认值
- 调参 + 聚合特征的组合将 R² 从 0.1839 提升至 0.2848，MAE 降低了 0.14 分
- 最终模型平均每条预测误差约 **1.9 分**（满分 20 分）

---

## 5. SHAP 可解释性分析

SHAP（SHapley Additive exPlanations）能够量化每个特征对每一条预测的具体贡献，比传统特征重要性更具解释力：不仅告诉你"哪个特征重要"，还告诉你"方向是什么"以及"影响了多少分"。

### 5.1 特征重要性排名

![SHAP条形图](<images/条形图.png>)

`failures`（不及格次数）以绝对优势排名第一，平均影响约 0.62 分，是最强的预测信号。

### 5.2 方向与分布分析

![SHAP Beeswarm](<images/Beeswarm 全图.png>)

**读图方式：**
- 每个点代表一个学生
- **颜色**（红/蓝）= 该学生在这个特征上的值高低
- **横轴位置** = 这个特征对该学生预测分数的影响方向和大小

**核心发现：**

| 特征 | 观察 | 解读 |
|------|------|------|
| `failures` | 蓝色点（低值）在右，红色点（高值）在左，最大拉低 -3 分 | 不及格次数越多，成绩预测越低，且影响非线性——从 0 次到 1 次的跌幅远大于从 2 次到 3 次 |
| `higher` | 蓝色点在左，红色点密集在 0 附近 | 有意愿读大学的学生（higher=1）预测分数更高 |
| `schoolsup` | 有补课（红）的学生 SHAP 值为负 | 反直觉现象：有校外辅导支持的学生成绩反而偏低。原因是**因果倒置**——是成绩差才去补课，不是补课导致成绩差 |
| `social_risk` | 点分布较散 | 社交风险对不同学生的影响不一致，存在非线性关系，这正是线性回归无法捕捉但 XGBoost 能处理的部分 |
| `parent_edu` / `alcohol` | 方向符合直觉，影响适中 | 父母教育水平越高，成绩略好；总饮酒量越高，成绩略低 |

---

## 6. 结论与建议

### 6.1 模型表现

最终 XGBoost 模型（调参 + 聚合特征）R² = 0.2848，平均预测误差 1.917 分。

R² 不高的原因并非模型选择问题，而是数据本身的限制：
- 样本量仅 634 条，统计功效有限
- 数据中包含大量 0 分（放弃考试），这类学生行为难以用常规特征预测
- 成绩是复杂社会现象，本数据集未包含的因素（如教师质量、学生智力、家庭突发事件）同样有重要影响

### 6.2 核心发现

影响学生成绩最关键的 5 个因素：

1. **`failures`（历史不及格次数）** — 最强预测因子，不及格记录越多，期末成绩越低，且早期干预窗口关键
2. **`absences`（缺课次数）** — 排名第二，缺课与成绩呈负相关，但影响存在非线性
3. **`school`（就读学校）** — GP 学校学生整体成绩高于 MS，可能反映学校资源或生源差异
4. **`higher`（升学意愿）** — 有明确升学意愿的学生成绩显著更好，动机是重要预测变量
5. **`social_risk`（社交风险评分）** — 高外出频率和高饮酒量组合对成绩有负向影响

### 6.3 给学校和教师的建议

基于以上分析，提出以下可操作建议：

**早期预警：**
- 将"历史不及格次数 ≥ 1"作为重点关注学生的触发条件，在学期初主动介入
- 建立缺课预警机制，缺课超过阈值时及时联系家长

**资源分配：**
- `schoolsup`（校外辅导）的反直觉结果提示：不应将补课资源平均分配，而应结合学生的**动机水平**（higher）同步评估。对于有升学意愿但暂时落后的学生，补课效果可能更显著
- 提升父母教育参与度（parent_edu 有正向影响），可通过家长工作坊等形式实现

**长期跟踪：**
- 当前模型在 0 分区间预测偏差较大，建议将"是否参加考试"作为独立分类问题单独建模，再对参考学生进行成绩回归预测

---

*分析工具：Python · pandas · scikit-learn · XGBoost · SHAP*  
*数据来源：[UCI Machine Learning Repository - Student Performance Dataset](https://archive.ics.uci.edu/ml/datasets/student+performance)*